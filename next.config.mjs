/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "www.example.com",
      "cdn.example.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
